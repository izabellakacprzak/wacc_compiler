# a simple function definition and call

# Output:
# 0

# Program:

begin
  int f(x, b) is
    bool a = x[1][2] == b[2] ;
    return 0 
  end
  int g() is
    char[] a = ['a', 'b', 'c'];
    char[] b = ['f', 'g', 'h'];
    char[][] c = [a,b] ;
    char[] d = ['a', 's'];
    int x = call f(c, d);
    return x
  end
  int x = call g();
  println 0
end
