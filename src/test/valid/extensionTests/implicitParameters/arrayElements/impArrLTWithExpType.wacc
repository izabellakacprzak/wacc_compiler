# a simple function definition and call

# Output:
# 0

# Program:

begin
  bool f(x, y) is
    bool a = x[2] < 'y' ;
    return a
  end
  char[] x = ['a', 'b', 'c'];
  bool b = call f(x, 5);
  println b
end
