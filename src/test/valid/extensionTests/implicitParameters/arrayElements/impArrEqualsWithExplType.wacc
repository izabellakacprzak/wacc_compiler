# a simple function definition and call

# Output:
# true

# Program:

begin
  bool f(a, b) is
    bool x =  b[2] == 'c';
    return x 
  end
  char[] chars = ['a', 'b', 'c', 'd' ];
  bool b = call f(8, chars);
  println b
end
