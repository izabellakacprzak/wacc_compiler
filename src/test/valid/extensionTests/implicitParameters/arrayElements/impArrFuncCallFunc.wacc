# a simple function definition and call

# Output:
# false

# Program:

begin
  bool f(x, b) is
    bool a = x[1][2] == b[1] ;
    return a 
  end
  bool g() is
    char[] a = ['a', 'b', 'c'];
    char[] b = ['f', 'g', 'h'];
    char[][] c = [a,b] ;
    char[] d = ['a', 's'];
    bool x = call f(c, d);
    return x
  end
  bool x = call g();
  println x
end
