# find the max of two numbers

# Output:
# max value = 17

# Program:

begin
  int i = 0 ;
  int x = 10 ;
  int y = 17 ;

  for(int i = 0; i < 3 ; i++) {

    println i
  }


    int i = 0
    jump to condition
    :body label
      body
      i++
    :condition label
      cond code


  while (y > 0 || x > 0) do
    x = x - 1 ;
    y = y - 1 ;
    i = i + 1
  done ;
  print "max value = ";
  println i
end
