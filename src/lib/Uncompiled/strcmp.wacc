begin
  int strcmp(char[] a, char[]b) is
  int i = 0;
  int j = 0;
  int lenA = len a - 1;
  int lenB = len b - 1;
  
  while i < lenA && j < lenB && a[i] == b [j] do
      i = i + 1;
      j = j + 1
  done;  
  return (ord a[i] - ord b[j]) - (ord b[j] - ord a[i]) 
  end
  skip
end  

